services:
  postgres:
    image: postgres:16-alpine
    container_name: crm_postgres
    environment:
      POSTGRES_USER: crm_user
      POSTGRES_PASSWORD: crm_password
      POSTGRES_DB: crm_db
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./service/docker/postgres-init:/docker-entrypoint-initdb.d
      - ./service/db/schema:/docker-entrypoint-initdb.d/schema:ro
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U crm_user -d crm_db"]
      interval: 10s
      timeout: 5s
      retries: 10
    restart: unless-stopped
    networks:
      - crm_network

  mongo:
    image: mongo:7
    container_name: crm_mongo
    ports:
      - "27017:27017"
    volumes:
      - mongo_data:/data/db
    healthcheck:
      test: ["CMD", "mongosh", "--quiet", "mongodb://localhost:27017/admin", "--eval", "db.runCommand({ ping: 1 }).ok"]
      interval: 10s
      timeout: 5s
      retries: 10
    restart: unless-stopped
    networks:
      - crm_network

  backend:
    build:
      context: .
      dockerfile: service/Dockerfile
    container_name: crm_backend
    environment:
      PORT: "4000"
      NODE_ENV: production
      DB_HOST: postgres
      DB_PORT: "5432"
      DB_USER: crm_user
      DB_PASSWORD: crm_password
      DB_NAME: crm_db
      JWT_SECRET: your-super-secret-jwt-key-change-this-in-production
      MONGO_URI: mongodb://mongo:27017
      MONGO_DB: crm_logs
      REQUEST_LOG_TTL_DAYS: "0"
      OWNER_DATA_RESET_ENABLED: "${OWNER_DATA_RESET_ENABLED:-true}"
      E2E_UI_DIR: /app/ui
      E2E_DB_NAME: crm_frontend_e2e_test
      E2E_DB_HOST: postgres
      E2E_DB_PORT: "5432"
      E2E_DB_USER: crm_user
      E2E_DB_PASSWORD: crm_password
      # Headed browser display streamed through the token-protected backend
      # viewer endpoint. Raw VNC/noVNC ports remain container-internal.
      E2E_LIVE_VIEW_ENABLED: "${E2E_LIVE_VIEW_ENABLED:-true}"
      E2E_HEADED: "${E2E_LIVE_VIEW_ENABLED:-true}"
      E2E_DISPLAY: "${E2E_DISPLAY:-:99}"
      E2E_SCREEN_GEOMETRY: "${E2E_SCREEN_GEOMETRY:-1440x900x24}"
      E2E_VIEWER_FRAME_ANCESTORS: "${E2E_VIEWER_FRAME_ANCESTORS:-'self' https://*.vercel.app}"
    depends_on:
      postgres:
        condition: service_healthy
      mongo:
        condition: service_healthy
    ports:
      # Host port is configurable to avoid conflicts on Windows/WSL (e.g. reserved/excluded ports).
      - "${BACKEND_PORT:-4000}:4000"
    restart: unless-stopped
    networks:
      - crm_network

  telegram-bot:
    build:
      context: ./bot
      dockerfile: Dockerfile
    container_name: crm_telegram_bot
    env_file:
      - path: ./bot/.env
        required: false
    environment:
      NODE_ENV: production
      DB_HOST: postgres
      DB_PORT: "5432"
      DB_USER: crm_user
      DB_PASSWORD: crm_password
      DB_NAME: crm_db
      BOT_DEFAULT_CENTER_ID: "${BOT_DEFAULT_CENTER_ID:-1}"
      BOT_REGISTRATION_CLASS_LABEL: "${BOT_REGISTRATION_CLASS_LABEL:-Unassigned}"
    depends_on:
      postgres:
        condition: service_healthy
      backend:
        condition: service_started
    restart: unless-stopped
    networks:
      - crm_network

volumes:
  postgres_data:
  mongo_data:

networks:
  crm_network:
    driver: bridge

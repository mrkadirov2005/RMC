import { Badge } from '@/components/ui/badge';
import { statusTone, type CalendarEvent } from '../calendarWorkspace';

export const AgendaCalendarView = ({ events, onSelect }: { events: CalendarEvent[]; onSelect: (event: CalendarEvent) => void }) => {
  const rows = [...events].sort((a, b) => `${a.date}${a.start_time}`.localeCompare(`${b.date}${b.start_time}`));
  return <div className="overflow-x-auto"><table aria-label="Calendar agenda" className="w-full min-w-[760px] text-sm"><thead className="bg-slate-50 text-left text-xs uppercase text-muted-foreground dark:bg-muted/30"><tr>{['Date', 'Time', 'Group', 'Teacher', 'Subject', 'Room', 'Status'].map(label => <th key={label} className="px-3 py-2">{label}</th>)}</tr></thead><tbody>{rows.length === 0 ? <tr><td colSpan={7} className="py-12 text-center text-muted-foreground">No upcoming lessons match the selected filters.</td></tr> : rows.map(event => <tr key={event.event_id} tabIndex={0} onClick={() => onSelect(event)} onKeyDown={key => key.key === 'Enter' && onSelect(event)} className="cursor-pointer border-t odd:bg-white even:bg-slate-50/80 hover:bg-primary/5 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-inset focus-visible:ring-ring dark:odd:bg-card dark:even:bg-muted/20"><td className="px-3 py-2 font-medium">{event.date}</td><td className="px-3 py-2 tabular-nums">{event.start_time.slice(0, 5)}–{event.end_time.slice(0, 5)}</td><td className="px-3 py-2 font-semibold">{event.class_name}</td><td className="px-3 py-2">{event.teacher_name || '—'}</td><td className="px-3 py-2">{event.subject_name || '—'}</td><td className="px-3 py-2">{event.room_name || '—'}</td><td className="px-3 py-2"><Badge className={statusTone(event.status)}>{event.status.replace('_', ' ')}</Badge></td></tr>)}</tbody></table></div>;
};

import { Input } from '@/components/ui/input';import { Select,SelectContent,SelectItem,SelectTrigger,SelectValue } from '@/components/ui/select';import { Button } from '@/components/ui/button';import type { CalendarFilters,CalendarResource } from '../calendarWorkspace';
export const CalendarWorkspaceFilters=({filters,resources,onChange,onClear}:{filters:CalendarFilters;resources:CalendarResource[];onChange:(f:CalendarFilters)=>void;onClear:()=>void})=>{const select=(type:CalendarResource['type'],key:keyof CalendarFilters,label:string)=><Select value={filters[key]||'all'} onValueChange={v=>onChange({...filters,[key]:v==='all'?'':v})}><SelectTrigger data-testid={`calendar-filter-${type}`} className="h-8 w-[145px] text-xs" aria-label={label}><SelectValue placeholder={label}/></SelectTrigger><SelectContent><SelectItem value="all">All {label}</SelectItem>{resources.filter(r=>r.type===type).map(r=><SelectItem key={r.id} value={r.id}>{r.name}</SelectItem>)}</SelectContent></Select>;return <div className="flex flex-wrap gap-2 border-b bg-slate-50/70 p-2 dark:bg-muted/20"><Input data-testid="calendar-filter-search" className="h-8 min-w-[180px] flex-1 text-xs" placeholder="Search group, teacher, subject or room" value={filters.query} onChange={e=>onChange({...filters,query:e.target.value})}/>{select('teacher','teacherId','teachers')}{select('class','classId','groups')}{select('subject','subjectId','subjects')}{select('room','roomId','rooms')}<Select value={filters.status||'all'} onValueChange={v=>onChange({...filters,status:v==='all'?'':v})}><SelectTrigger data-testid="calendar-filter-status" className="h-8 w-[135px] text-xs" aria-label="status"><SelectValue/></SelectTrigger><SelectContent><SelectItem value="all">All statuses</SelectItem>{['planned','ready','in_progress','conducted','cancelled','conflict'].map(s=><SelectItem key={s} value={s}>{s.replace('_',' ')}</SelectItem>)}</SelectContent></Select><Button data-testid="calendar-filter-clear" size="sm" variant="ghost" onClick={onClear}>Clear</Button></div>};

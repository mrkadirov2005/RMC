VITE_API_BASE_URL=https://api-temurbek.e-tizim.uz/

# Required to enable owner self-registration. Must match the backend's OWNER_INVITE_KEY.
# Leave unset to keep the register page disabled.
VITE_OWNER_INVITE_KEY=
